apiVersion: v1
kind: Service
metadata:
  name: kruise-game-dashboard-frontend
  labels:
    app: kruise-game-dashboard-frontend
spec:
  selector:
    app: kruise-game-dashboard-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  clusterIP: None